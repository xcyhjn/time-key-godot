# Normalize EOL for all files that Git considers text files.
* text=auto eol=lf

# 这些 Godot 脚本原本使用 CRLF；保持原换行，避免 GitHub Desktop 把整文件显示成换行变化。
scene/card/custom_card.gd text eol=crlf
scene/in_scene/DragShapeController.gd text eol=crlf
scene/in_scene/timeline/timeline_ui.gd text eol=crlf
scene/in_scene/timeline/TimelineManager.gd text eol=crlf
